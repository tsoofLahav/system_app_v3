# Cursor canvases (repo copies)

Interactive diagrams for Cursor Canvas. **Open the live copy** from Cursor’s project canvases folder (beside chat). This folder keeps a **versioned snapshot** in git.

| Diagram | Repo file | Live (Cursor) |
|---------|-----------|---------------|
| App areas map | [`app-areas-map.canvas.tsx`](app-areas-map.canvas.tsx) | `~/.cursor/projects/Users-tsoof-dev-system-app/canvases/app-areas-map.canvas.tsx` |

After editing the live canvas, copy it back here so the repo stays in sync.
